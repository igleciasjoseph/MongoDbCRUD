<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css")
        title Document
    body.container.text-center
        h1.mt-5 #{animal.name}
        hr.mb-5
        div
            h3 Stats:
        div.mt-5 
            p Name: #{animal.name}
            p Type of: #{animal.typeOf}
            p Age: #{animal.age}
            p Color: #{animal.color}
            p.mb-5 Time Submitted: #{animal.time}
            a(href=`/animal/edit/` + animal._id).btn.btn-primary.mb-5 Edit
        a(href="/").btn.btn-primary Back Home